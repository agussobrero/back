/* import { MensajeDTO } from "../dto/mensaje.dto"

export interface MensajeDAOInterface {
    getAll()
    getById(id: string)
    save(mensaje: MensajeDTO)
    update(id: string, mensaje: MensajeDTO)
    delete(id: string)
} */