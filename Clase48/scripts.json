{
  "$schema": "https://deno.land/x/denon@2.5.0/schema.json",
  "scripts": {
    "start": "deno run --allow-net --allow-read --allow-env index.ts",
    "install": "curl -fsSL https://deno.land/x/install/install.sh | sh", 
    "export": "export PATH=/Users/AguS/.deno/bin:$PATH"
  },
  "deno.enable": true,
  "deno.suggest.imports.host": {
    "https://deno.land": true
  },
  "compilerOptions": {
    "allowImportingTsExtensions": "iKnowWhatImDoing",
    "moduleResolution": "bundler"
  }
}